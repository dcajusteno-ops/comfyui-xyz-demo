# ComfyUI XYZ Demo 进阶功能开发任务书 (按优先级排列)

本任务书已根据开发逻辑、系统稳定性和用户体验影响进行了优先级排序。

---

## P1. 后端连接状态感知 (System Stability)
**目标**：提升系统健壮性，实时反馈 ComfyUI 后端的健康状况，防止无效操作。

### 核心任务
- [x] **状态监控器**：在 `ComfyClient` 中利用 WebSocket `onopen`/`onclose` 事件实时监测连接。
- [x] **UI 指示灯**：在顶部导航栏增加连接状态图标（绿/黄/红）。
- [x] **断连全屏蒙版**：当连接彻底断开时，弹出优雅的全局遮罩层，引导用户检查后端端口或重启服务。
- [x] **心跳检测**：引入定时心跳（Ping/Pong）机制，防止 WebSocket 被静默关闭。

---

## P2. 侧边栏折叠与布局优化 (UX/UI)
**目标**：优化小屏幕下的使用体验，提供更大的画布与结果预览视野。

### 核心任务
- [x] **折叠切换器**：在侧边栏底部增加折叠按钮。
- [x] **迷你模式 UI**：当侧边栏折叠时，仅显示功能图标，隐藏文字标签。
- [x] **响应式自适应**：在屏幕宽度小于 1024px 时自动进入折叠状态。
- [x] **CSS 动画优化**：为侧边栏的展开/折叠增加平滑的过渡效果。

---

## P3. 动态预览轴 (XYZ Plot Preview)
**目标**：在启动 XYZ 批量任务前，允许用户预览并手动过滤参数组合，节省算力。

### 核心任务
- [x] **UI 预览面板**：在 XYZ 控制器下方新增“组合预览”列表，展示即将生成的 N 个组合。
- [x] **手动勾选逻辑**：为每个组合增加复选框，用户可以取消勾选不想运行的组合。
- [x] **逻辑重构**：修改 `buildXyzCombinations` 函数，支持过滤被排除的组合。
- [x] **估时显示**：根据组合数量初步估算所需的总批次数。

---

## P4. 多人画布融合算法 (Advanced Feature)
**目标**：提升多人工作流中角色重叠区域的自然度与可控性。

### 核心任务
- [ ] **融合模式选择**：在多人面板增加“融合算法”下拉框（如：Mask 叠加、Latent 融合）。
- [ ] **Mask 羽化控制**：为每个角色蒙版增加独立的羽化（Feathering）参数。
- [ ] **重叠区域处理**：重构 `multiCanvas.ts`，支持计算重叠区域并自动调整权重。
- [ ] **预览可视化**：在画布上直观显示羽化边缘和融合区域分布。
