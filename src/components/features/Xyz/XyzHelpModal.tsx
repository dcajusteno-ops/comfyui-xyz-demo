import { ModalFrame } from "../../ui";

export function XyzHelpModal({ onClose }: { onClose: () => void }) {
  return (
    <ModalFrame title="XYZ 控制器怎么用" onClose={onClose}>
      <div className="help-body">
        <p>
          XYZ 会按 X、Y、Z 三个轴组合参数，然后顺序提交到 ComfyUI。X 轴变化最快，适合 seed；Y/Z 适合
          CFG、Steps、尺寸或 LoRA 强度。
        </p>
        <p>
          取值支持两种写法：逗号或换行枚举，例如 `5,7,9`；数值范围，例如 `20..40..10` 表示 20、30、40（起..止..步进）。
          缺省步进：整数端点为 1（`1..5` = 1～5），小数端点为 0.1——`0.4..1.0` = 0.4、0.5、…、1.0，正是 LoRA
          强度、重绘幅度的常用扫描粒度。范围可与枚举值混填（如 `0.2, 0.4..1.0`），对所有数值轴生效（强度、CFG、步数、seed 等）。
        </p>
        <p>
          {
            "“LoRA 模型”轴的批量写法：① 模型名 + 序号范围：`模型名{1..4}` 直接拼出 模型名1…4，支持步进 `模型名{1..6..2}`（= 1、3、5）——拼出的名字要与实际文件名完全一致（含空格）；② 通配匹配：`模型名*` 匹配库里所有以此开头的文件，`*关键词*` 包含匹配，`?` 单个字符；③ 库序号：`1..6` = 列表第 1～6 个，`{2..4}` 等效，`*` = 已加载的全部（最多 256 个）。写法之间可以和文件名混填（逗号分隔）。匹配/校验范围是 LoRA 管理器当前列表（往下滚动加载后更全）。"
          }
        </p>
        <p>
          “正向追加”会把轴值追加到正向提示词；多人模板中会追加到全局 prompt。“LoRA
          强度”可以动态调节当前模板中已选择的对应 LoRA 权重。
        </p>
      </div>
    </ModalFrame>
  );
}
